# ============================================================
# Photodate - Configuratiebestand
# ============================================================
#
# STAP 1: Kopieer dit bestand naar .env
#   Op je Synology (via SSH):  cp .env.example .env
#   Op Windows:                copy .env.example .env
#   Op Mac/Linux:              cp .env.example .env
#
# STAP 2: Open het .env bestand in een teksteditor en vul je
#   waardes in. Verwijder NIET de aanhalingstekens.
#
# STAP 3: Sla het bestand op. NOOIT committen naar git!
#   (het staat al in .gitignore)
#
# LET OP: Na wijzigingen moet je de container herstarten:
#   docker compose down && docker compose up -d
# ============================================================

# --- OpenAI API Key ---
# Haal je key op via: https://platform.openai.com/api-keys
# 1. Log in op platform.openai.com
# 2. Ga naar "API keys" in het menu links
# 3. Klik "Create new secret key"
# 4. Kopieer de key en plak hem hieronder (begint met sk-)
OPENAI_API_KEY=sk-PLAK-HIER-JE-KEY

# --- Pad naar je foto's ---
# Dit is het pad op je Synology naar de map met fotoalbums.
# Elke submap wordt als apart album getoond in de web interface.
#
# Voorbeeld mapstructuur:
#   /volume1/photos/
#   ├── Vakantie Spanje 1987/
#   │   ├── scan_001.jpg
#   │   ├── scan_002.jpg
#   │   └── ...
#   ├── Bruiloft Jan en Marie/
#   │   ├── foto_001.jpg
#   │   └── ...
#   └── ...
PHOTOS_PATH=/volume1/photos
